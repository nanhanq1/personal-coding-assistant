# Project Requirements

## 项目定义

最终项目是一个工业级 **Personal Coding Assistant**，同时具备 Coding Agent 和 Personal Assistant 能力。它不是 demo，不以“能跑通 happy path”为完成标准；完成标准是可运行、可测试、可审计、可解释、可维护，并能作为作品集展示。

## 产品目标

用户可以在一个本地受控工作区中提出代码任务。系统能够理解仓库结构，选择相关上下文，调用工具读取和修改代码，生成 diff，运行测试和质量检查，在危险操作前请求审批，并把任务轨迹、决策、学习进度和用户偏好沉淀到长期记忆。

## 核心用户故事

1. 作为学习者，我希望 Agent 能解释当前项目结构、当前任务调用链和风险边界，帮助我理解而不是只给最终代码。
2. 作为开发者，我希望 Agent 能读取仓库、定位相关文件、提出修改计划、生成局部 patch、运行测试并输出可审查 diff。
3. 作为项目维护者，我希望所有文件写入、命令执行、git 操作和敏感操作都有权限策略、审计日志和可回放轨迹。
4. 作为长期用户，我希望 Agent 记住我的偏好、项目决策、学习进度、未完成任务和复盘结论，并在后续任务中安全检索。
5. 作为作品集评审者，我希望通过 README、架构文档、测试报告、评估报告和演示脚本判断项目具备真实工程质量。

## 功能需求

### Coding Agent

- 读取代码仓库，生成 repo map。
- 建立代码符号索引和文件相关性排序。
- 构建任务上下文，控制 token/内容预算。
- 执行工具调用，包括文件读取、局部编辑、命令执行、搜索、测试、lint、type check 和 git 操作。
- 生成 diff、审查 diff、解释改动风险。
- 自动运行测试并根据失败输出进入修复循环。
- 生成 commit message，但提交必须经过权限策略或用户确认。
- 支持 sandbox 或受控命令执行，并有 timeout、输出截断、敏感信息脱敏和审计日志。

### Personal Assistant

- 维护长期记忆：用户偏好、项目记忆、学习进度、每日任务、架构决策、复盘问题。
- 支持知识库读取：Markdown、文本、代码文档，后续可扩展 PDF/网页。
- 支持上下文压缩：把长轨迹压缩成可追溯摘要。
- 支持任务记录：计划、当前状态、阻塞项、下一步。
- 支持个人状态图：实体、关系、事件、时间范围和证据来源。
- 支持记忆写入策略：候选记忆、用户确认、冲突处理、过期/更新。

### 工业级工程能力

- 清晰模块边界：core、tools、runtime、permissions、coding、context、retrieval、memory、graph、evaluation、observability、cli。
- 可测试架构：每个模块至少有单元测试，关键链路有集成测试，最终有 E2E 场景。
- 类型检查、lint、格式检查和 CI。
- 配置系统、密钥边界和本地环境隔离。
- 结构化日志、trace_id、审计日志、回放和评估报告。
- 文档体系：需求、架构、ADR、学习路线、实现日志、评估、发布清单、作品集说明。

## 非功能需求

| 维度 | 要求 |
|---|---|
| 安全性 | 默认拒绝高风险操作；写文件、shell、git、外部网络和密钥相关能力必须有策略边界 |
| 可观测性 | 每次 agent run、tool call、permission decision、memory write 都可按 trace_id 追踪 |
| 健壮性 | 坏输入、工具失败、超时、上下文过大、检索无结果、测试失败都必须有稳定错误语义 |
| 可测试性 | 核心模块单测覆盖正常路径和失败路径；E2E 覆盖真实小仓库修改 |
| 可维护性 | 模块依赖方向清晰，不能让 AgentLoop 直接依赖所有具体工具和业务细节 |
| 可解释性 | 每个阶段都能用 Mermaid 图、测试和文档解释“为什么这么设计” |

## 验收定义

项目完成时必须满足：

1. `python -m pytest -q`、lint、type check、关键示例和 E2E 场景全部通过。
2. 能在一个真实小型 Python 仓库中完成至少 3 类任务：读代码解释、局部修改并跑测试、生成 diff 并提交候选 commit。
3. 危险命令、覆盖写入、git commit/push、外部网络、密钥读取等操作不会默认静默执行。
4. 长期记忆能保存和检索项目偏好、学习进度、决策记录和任务状态，并有写入策略测试。
5. RAG 能对项目文档和个人知识库进行检索、引用、评估。
6. Observability 能按 trace_id 查看一次任务的 LLM 消息、工具调用、权限决策、测试结果和记忆写入。
7. README 和作品集文档准确说明已实现能力、未实现能力、架构取舍和验证证据。

## 明确不做

- 不做多用户 SaaS 权限系统。
- 不做企业级密钥管理平台，只做本地 secret 边界和不泄漏策略。
- 不依赖真实 LLM 才能完成早期核心测试；真实 adapter 在后续阶段引入。
- 不复制 Aider、OpenHands、Cline、Khoj、Letta 或 LangGraph 的整体架构，只抽象必要能力。
